import 'package:flutter/material.dart';

class DetailKulinerNusantara extends StatelessWidget {
  const DetailKulinerNusantara({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Detail Kuliner Nusantara'),
    );
  }
}
